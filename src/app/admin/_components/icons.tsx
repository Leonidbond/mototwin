/**
 * Re-exports of the lucide-react icons used across the admin panel.
 * Centralizing makes it easy to swap or theme later, and avoids leaking
 * library imports to every component.
 */
export {
  AlertTriangle,
  ArrowDownRight,
  ArrowUpRight,
  Bell,
  Bike,
  Boxes,
  CalendarDays,
  ChevronDown,
  ChevronRight,
  CircleAlert,
  Combine,
  CreditCard,
  ExternalLink,
  FileText,
  Filter,
  ListTodo,
  Plus,
  RefreshCcw,
  Search,
  ShieldCheck,
  Upload,
  Users,
  Wrench,
  XOctagon,
} from "lucide-react";

/** Alias kept for KPI card semantics (verified parts). */
export { ShieldCheck as CheckShield } from "lucide-react";
