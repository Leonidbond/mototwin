# Copy to keystore.properties (gitignored) after generating the release keystore.
# See apps/app/scripts/generate-android-release-keystore.sh
#
# storeFile is relative to apps/app/android/
storeFile=keystores/mototwin-release.keystore
storePassword=CHANGE_ME
keyAlias=mototwin-release
keyPassword=CHANGE_ME
