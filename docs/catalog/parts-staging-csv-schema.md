# Superseded

This document is superseded by **[parts-catalog-schema.md](./parts-catalog-schema.md)**.

The unified contract covers **39** staging columns (28 base + 11 extended), companion CSV files, enums, validation, DB mapping, CLI import, and admin bulk import with CSV template download (`GET /api/admin/imports/template`).
